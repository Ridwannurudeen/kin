node_modules/
.env
.env.local
.env.production

# Wallets and keys — NEVER commit
.demo-wallets.json
.persona-wallets.json
demo/.wallets.json
demo/.hunter-wallets.json
*.key
*.bin
wallet.key
private.txt
hunter.lock

# Build artifacts
artifacts/
cache/
typechain-types/
.openzeppelin/
*.log

# bin/post-deploy.sh symlinks deployments/ -> public/deployments/ so the static
# server can serve /deployments/Hunt.json from the public/ root. Symlink is
# environment-local; never commit it.
public/deployments
